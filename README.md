# 🌐 Linux Universal DNS Changer (一键 DNS 修改脚本)

这是一个轻量级、智能化的 Linux DNS 修改脚本。它可以自动检测系统环境，将系统的 DNS 服务器永久修改为 **Google (8.8.8.8)** 和 **Cloudflare (1.1.1.1)**，并防止 DHCP 自动覆盖。

## 🚀 快速使用 (Quick Start)

无需下载文件，直接在服务器终端执行以下命令即可：

```bash
curl -fsSL [https://raw.githubusercontent.com/ike666888/DNS/main/DNS.sh](https://raw.githubusercontent.com/ike666888/DNS/main/DNS.sh) | sudo bash
